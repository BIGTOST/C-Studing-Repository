#include <stdio.h>

void main()
{
  /*
   * Uma das funções que permite que texto seja escrito na tela é a função printf
   *  prinf= print + f (f de formatado) 
   *
   * Como referido no prog0101 palavras seguidas de parenteses significa que esta palavra é
   * reservada e e chama uma função, neste caso dentro dos parenteses de ver ser colocado
   * o texto que queremos expor
   */
  printf("Hello World"); // esta linha é responsa por  apresnetar a mensagem  que queremos imprimir
  //importante referir que todas as instruções devem ser acabadas com ;
  // outra importante menção é que o caracter aspas(") é o unico caracter que não pode ser 
  // substituido por aspas  simples (') ou duas aspas simples('')
 
