#include <stdio.h>

main()
{
	/*
	 * A diferença entre o puts e o printf é basicamente que no puts não permite formatação 
	 * pelomenos não tanta como o printf, puts basicamente representa put stringm:coloque a string
	 * escreve na forma mais literal que é introduzido, entaõ pode servir melhor para quando queremos motrar
	 * somente a string mesmo as diferenças no binario sendo pifias podem ajudar caso necessitemos
	 * poupar o maximo possivel.
	 */
	puts("Hello World");
}
